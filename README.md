# hrms-service

## for running hrms application

  ```
  cd ~\hrms-service\docker
  ```
  ```
  docker-compose up
  ```
 
 
 ## if you want to re-build docker image;
 
  ```
  cd ~\hrms-service
  ```
  ```
  mvnw install
  ```
 
